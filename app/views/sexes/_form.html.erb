<%= form_with(model: sex, local: true) do |form| %>
  <% if sex.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sex.errors.count, "error") %> prohibited this sex from being saved:</h2>

      <ul>
        <% sex.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :libelle %>
    <%= form.text_field :libelle %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
