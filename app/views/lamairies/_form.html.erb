<%= form_with(model: lamairy, local: true) do |form| %>
  <% if lamairy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lamairy.errors.count, "error") %> prohibited this lamairy from being saved:</h2>

      <ul>
        <% lamairy.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
